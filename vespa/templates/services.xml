<?xml version="1.0" encoding="UTF-8"?>
<services version="1.0">
    {% if configurations %}
    {% for config in configurations %}
    {% autoescape off %}
    {{ config.to_text }}
    {% endautoescape %}
    {% endfor %}
    {% endif %}
    <container id="{{ application_name }}_container" version="1.0">
        {% if schemas %}
        <search></search>
        <document-api></document-api>
        <document-processing></document-processing>
        {% endif %}
        {% if search_clusters %}
        {% endif %}
        {% if components %}
        {% for component in components %}
        {% autoescape off %}
        {{ component.to_xml_string(2) }}
        {% endautoescape %}
        {% endfor %}
        {% endif %}
        {% if auth_clients %}
        <clients>
        {% for client in auth_clients %}
        {% autoescape off %}
            {{ client.to_xml_string(3) }}
        {% endautoescape %}
        {% endfor %}
        </clients>
        {% endif %}
        {% if stateless_model_evaluation %}
        <model-evaluation/>
        {% endif %}
    </container>
    {% if schemas %}
    <content id="{{ application_name }}_content" version="1.0">
        <redundancy>1</redundancy>
        <documents>
            {% set document_processing_added = false %}
            {% for schema in schemas %}
            {% if schema.global_document %}
            <document type="{{ schema.name }}" mode="index" global="true"></document>
            {% else %}
            <document type="{{ schema.name }}" mode="{{ schema.mode }}"></document>
            {% endif %}
            {% if schema.mode == "streaming" and not document_processing_added %}
            <document-processing chain="indexing" cluster="default" />
            {% set document_processing_added = true %}
            {% endif %}
            {% endfor %}
        </documents>
        <nodes>
            <node distribution-key="0" hostalias="node1"></node>
        </nodes>
    </content>
    {% endif %}
</services>