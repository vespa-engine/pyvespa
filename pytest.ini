# pytest.ini
[pytest]
pythonpath = .
markers =
    performance: performance/load tests that invoke k6 against Vespa endpoints
    perf_last: performance test that must run after all other tests in the session
